# Release Notes — coffeemix_all 0.2

## 0.2.0

- Promoted coffeemix_all 0.2 as a stable OpenCode + OMO workspace package.
- Keeps OMO / oh-my-openagent as the baseline control plane.
- Preserves all 14 protected `gb-*` specialists.
- Ships 8 documented local workflow skills.
- Includes a safer PowerShell installer with `-WhatIf`, `-ConfigDir`, OMO/OpenAgent config detection, plugin skip behavior, and JSON write protection during preview mode.
- Includes CoffeeMix UI badge plugin support.
- Keeps release docs focused on installation and release notes.
- Rejects OMO replacement, copied router/model/loader logic, default GBrain/browser/checkpoint/design runtime, `gb-*` deletion, and full upstream parity claims.
