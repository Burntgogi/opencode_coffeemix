# 릴리스 노트 — coffeemix_all 0.2

## 0.2.0

- coffeemix_all 0.2를 OpenCode + OMO용 안정 배포 패키지로 승격했다.
- OMO / oh-my-openagent를 baseline control plane으로 유지한다.
- 보호 대상 `gb-*` specialist 14개를 모두 유지한다.
- 문서화된 local workflow skill 8개를 포함한다.
- `-WhatIf`, `-ConfigDir`, OMO/OpenAgent config 감지, plugin skip 동작, preview mode JSON write 보호를 갖춘 안전한 PowerShell installer를 포함한다.
- CoffeeMix UI badge plugin을 포함한다.
- 배포 문서는 설치와 릴리스 노트 중심으로 정리했다.
- OMO replacement, copied router/model/loader logic, 기본 GBrain/browser/checkpoint/design runtime, `gb-*` 삭제, full upstream parity claim을 거부한다.
